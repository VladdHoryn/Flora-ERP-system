server:
  port: ${SERVER_PORT:8083}

spring:
  datasource:
    username: ${DB_USERNAME}
    #    password: ${DB_PASSWORD}
    url: ${DB_URL:jdbc:mariadb://sales-db:3306/sales_service_db}

  flyway:
    url: ${DB_URL}
    user: ${DB_USERNAME}
    password: ${DB_PASSWORD}

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka